# Changelog

All notable changes to `additional_smtp` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `additional_smtp::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [0.2.0] - 2026-04-11

- Formalized the plugin's self-metadata through `additional_smtp::PLUGIN_VERSION` and `additional_smtp::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
